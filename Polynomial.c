#include <stdio.h>

int main (void)
{
    int x, Poly;
    printf("Enter Value of x: ");
    scanf("%d", &x);
    printf("The value of the Polynomial is %d\n", Poly = ((((3 * x + 2) * x - 5) * x - 1) * x + 7) * x - 6); // Technique for evaluat­ing polynomials known as Horner’s Rule
}